# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# Values are deliberately distinct: the generated "MyString" for every record
# makes any assertion about page content meaningless.

one:
  name: Primary Account

two:
  name: Secondary Account
